# Azure Storage Account Configuration
# Using Managed Identity - no account keys needed in production
# For local development, use Azure CLI: az login

# Your Azure Storage Account Name
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name

# Container names
AZURE_STORAGE_CONTAINER_NAME=files
AZURE_STORAGE_TRASH_CONTAINER_NAME=trash

# Azure AD Authentication Configuration
# Create an App Registration in Azure Portal → Azure Active Directory → App registrations

# Azure AD Application (client) ID
AZURE_AD_CLIENT_ID=your-client-id

# Azure AD Client Secret (from Certificates & secrets)
AZURE_AD_CLIENT_SECRET=your-client-secret

# Azure AD Directory (tenant) ID
AZURE_AD_TENANT_ID=your-tenant-id

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# Trash Cleanup API Key (for scheduled task)
CLEANUP_API_KEY=your-cleanup-api-key
